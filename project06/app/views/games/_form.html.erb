<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<table>
	<tr>
		<div class="field">
			<td><%= f.label :title %></td>
			<td><%= f.text_field :title %></td>
		</div>
	</tr>
	<tr>
		<td />
		<td>
			<div class="field">
				<% Game::RATINGS.each do |ra| %>
					<%= f.radio_button :rating, ra.to_s %> <%= f.label :rating, ra.to_s %><br />
				<% end %>
			</div>
		</td>
	</tr>
	<tr>
		<td />
		<td>
			<div class="actions">
				<%= f.submit "Save" %>
			</div>
		</td>
	</table>
<% end %>
